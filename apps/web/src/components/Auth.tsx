/**
 * Auth.tsx
 *
 * DEPRECATED: Custom auth UI has been moved to apps/web/src/pages/LoginPage.tsx
 * This file is kept for backward compatibility in case it's referenced elsewhere.
 *
 * The custom auth implementation includes:
 * - Custom login/signup forms with validation
 * - Google OAuth integration
 * - Email confirmation flow
 * - Fully styled to match Mochi's pastel aesthetic
 */

export function AuthComponent() {
  throw new Error(
    'AuthComponent is deprecated. Use the custom auth UI in LoginPage.tsx instead.'
  )
}